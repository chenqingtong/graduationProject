# 简化审批流程设计文档

## 设计目标

设计一个**不依赖 Flowable 流程定义（definition_id）**的简化审批流程，使得：
1. 发起审批时不需要流程定义
2. 审批人可以在"我的事务"的"待办任务"中看到审批任务
3. 审批人可以完成审批（通过/拒绝）
4. 简化数据库设计，减少对 Flowable 引擎的依赖

## 数据库设计

### 1. 新增表：`pmhub_wf_approval_task`（简化审批任务表）

**表结构：**
```sql
CREATE TABLE `pmhub_wf_approval_task` (
  `id` varchar(64) NOT NULL COMMENT '审批任务ID',
  `extra_id` varchar(64) NOT NULL COMMENT '关联的业务ID（如任务ID、项目ID等）',
  `type` varchar(32) NOT NULL COMMENT '审批类型（task/project等）',
  `title` varchar(255) DEFAULT NULL COMMENT '审批任务标题',
  `url` varchar(1000) DEFAULT NULL COMMENT '详情页URL',
  `status` varchar(20) NOT NULL DEFAULT 'pending' COMMENT '审批状态：pending-待审批, approved-已通过, rejected-已拒绝, cancelled-已取消',
  `approver_id` varchar(64) NOT NULL COMMENT '审批人用户ID',
  `approver_name` varchar(100) DEFAULT NULL COMMENT '审批人姓名',
  `approver_type` varchar(20) NOT NULL COMMENT '审批人类型：user-指定用户, role-角色, dept-部门',
  `approver_value` varchar(255) DEFAULT NULL COMMENT '审批人值（用户ID/角色ID/部门ID，多个用逗号分隔）',
  `initiator_id` varchar(64) NOT NULL COMMENT '发起人用户ID',
  `initiator_name` varchar(100) DEFAULT NULL COMMENT '发起人姓名',
  `approval_comment` text DEFAULT NULL COMMENT '审批意见',
  `approval_time` datetime DEFAULT NULL COMMENT '审批时间',
  `created_by` varchar(64) DEFAULT NULL COMMENT '创建人',
  `created_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_by` varchar(64) DEFAULT NULL COMMENT '更新人',
  `updated_time` datetime DEFAULT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`) USING BTREE,
  KEY `idx_extra_id_type` (`extra_id`, `type`) USING BTREE,
  KEY `idx_approver_id` (`approver_id`) USING BTREE,
  KEY `idx_status` (`status`) USING BTREE,
  KEY `idx_created_time` (`created_time`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='简化审批任务表（不依赖Flowable流程定义）';
```

**字段说明：**
- `id`: 审批任务唯一标识
- `extra_id`: 关联的业务对象ID（如任务ID）
- `type`: 审批类型（task/project等）
- `title`: 审批任务标题（如"任务审批：XXX"）
- `url`: 详情页URL，用于跳转到业务详情页
- `status`: 审批状态
  - `pending`: 待审批
  - `approved`: 已通过
  - `rejected`: 已拒绝
  - `cancelled`: 已取消
- `approver_id`: 审批人用户ID（单个审批人）
- `approver_name`: 审批人姓名（冗余字段，便于查询显示）
- `approver_type`: 审批人类型
  - `user`: 指定用户
  - `role`: 角色
  - `dept`: 部门
- `approver_value`: 审批人值（用于支持多个审批人或角色/部门）
- `initiator_id`: 发起人用户ID
- `initiator_name`: 发起人姓名（冗余字段）
- `approval_comment`: 审批意见
- `approval_time`: 审批时间

### 2. 修改表：`pmhub_project_task_process`

**新增字段：**
```sql
ALTER TABLE `pmhub_project_task_process` 
ADD COLUMN `approval_task_id` varchar(64) DEFAULT NULL COMMENT '简化审批任务ID（当instance_id为空时使用）' AFTER `task_id`;
```

**说明：**
- 当 `instance_id` 为空且 `definition_id` 为空时，使用简化审批流程
- `approval_task_id` 关联到 `pmhub_wf_approval_task` 表

### 3. 表关系说明

```
pmhub_wf_approval_set (审批设置)
  ├── extra_id (任务ID)
  └── approval_info (审批人信息JSON)

pmhub_project_task_process (审批流程记录)
  ├── extra_id (任务ID)
  ├── instance_id (Flowable流程实例ID，可为空)
  ├── definition_id (流程定义ID，可为空)
  └── approval_task_id (简化审批任务ID，当instance_id为空时使用)

pmhub_wf_approval_task (简化审批任务)
  ├── id (审批任务ID)
  ├── extra_id (任务ID)
  └── approver_id (审批人ID)
```

## 流程设计

### 1. 审批流程启动逻辑

**判断条件：**
- 如果 `definition_id` 不为空：使用 Flowable 流程引擎（原有逻辑）
- 如果 `definition_id` 为空：使用简化审批流程（新逻辑）

**简化审批流程启动步骤：**
1. 检查任务状态（必须是已完成状态）
2. 查询审批设置（`pmhub_wf_approval_set`）
3. 解析审批人信息（`approval_info` JSON）
4. 根据审批人类型创建审批任务：
   - **指定用户（单个）**：创建一条审批任务记录
   - **指定用户（多个）**：为每个用户创建一条审批任务记录
   - **角色/部门**：查询该角色/部门下的所有用户，为每个用户创建一条审批任务记录
5. 创建或更新 `pmhub_project_task_process` 记录，设置 `approval_task_id`
6. 更新任务状态为"审批中"

### 2. 待办任务查询逻辑

**修改 `selectTodoProcessList()` 方法：**
1. 查询 Flowable 待办任务（原有逻辑）
2. 查询简化审批待办任务：
   ```sql
   SELECT * FROM pmhub_wf_approval_task 
   WHERE approver_id = ? 
     AND status = 'pending'
     AND (approver_type = 'user' 
          OR approver_id IN (SELECT user_id FROM sys_user_role WHERE role_id IN (...))
          OR approver_id IN (SELECT user_id FROM sys_user WHERE dept_id IN (...)))
   ORDER BY created_time DESC
   ```
3. 合并两种类型的待办任务
4. 统一返回格式

### 3. 审批完成逻辑

**审批通过：**
1. 更新审批任务状态为 `approved`
2. 记录审批意见和审批时间
3. 检查是否所有审批人都已审批（如果是多人审批）
4. 如果所有审批人都已通过，更新业务对象状态
5. 更新 `pmhub_project_task_process` 的 `approved` 字段为 "1"

**审批拒绝：**
1. 更新审批任务状态为 `rejected`
2. 记录审批意见和审批时间
3. 更新所有相关审批任务状态为 `cancelled`（取消其他待审批任务）
4. 更新业务对象状态
5. 更新 `pmhub_project_task_process` 的 `approved` 字段为 "3"（拒绝）

## 代码实现要点

### 1. 实体类

创建 `WfApprovalTask.java` 实体类，对应 `pmhub_wf_approval_task` 表。

### 2. Mapper 接口

创建 `WfApprovalTaskMapper.java`，包含以下方法：
- `selectTodoListByUserId()`: 查询用户的待办审批任务
- `selectByExtraIdAndType()`: 根据业务ID和类型查询审批任务
- `updateStatus()`: 更新审批状态

### 3. Service 层

在 `WfProcessServiceImpl` 中新增方法：
- `createSimplifiedApprovalTasks()`: 创建简化审批任务
- `selectSimplifiedTodoList()`: 查询简化审批待办任务
- `completeSimplifiedApproval()`: 完成简化审批

### 4. Controller 层

在 `WfProcessController` 中新增接口：
- `POST /workflow/process/completeSimplifiedApproval`: 完成简化审批

## 优势

1. **简化流程**：不需要创建 Flowable 流程定义，直接基于审批设置创建审批任务
2. **灵活性**：支持单个/多个用户、角色、部门等多种审批人类型
3. **可扩展**：可以轻松扩展支持会签、或签等复杂审批场景
4. **性能**：减少对 Flowable 引擎的依赖，提高性能
5. **兼容性**：保留原有 Flowable 流程，两种方式可以并存

## 迁移方案

1. **数据库迁移**：执行 `sql/simplified_approval_design.sql`
2. **代码部署**：部署新的代码逻辑
3. **数据迁移**：对于现有的无流程定义的审批，可以批量创建简化审批任务
4. **兼容处理**：代码中判断 `definition_id` 是否为空，决定使用哪种审批方式

## 注意事项

1. **审批人类型处理**：
   - `USERS`: 直接使用用户ID
   - `ROLES`: 需要查询角色下的所有用户
   - `DEPTS`: 需要查询部门下的所有用户
   - `INITIATOR`: 使用流程发起人ID

2. **多人审批处理**：
   - 方案1：会签（所有人通过才算通过）
   - 方案2：或签（任意一人通过即通过）
   - 当前设计支持会签，可以扩展支持或签

3. **审批顺序**：
   - 当前设计不支持审批顺序，所有审批人同时收到审批任务
   - 如需支持顺序审批，可以添加 `approval_order` 字段

4. **审批历史**：
   - 审批完成后，审批任务记录保留，可作为审批历史查询
   - 可以添加 `pmhub_wf_approval_history` 表存储更详细的审批历史

