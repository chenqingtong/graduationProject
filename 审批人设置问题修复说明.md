# 审批人设置问题修复说明

## 问题分析

### 1. 问题描述
虽然在前端选择了审批人并设置了审批信息，但设置只在前端完成，没有保存到后端数据库。

### 2. 根本原因
**数据库表 `pmhub_wf_approval_set` 缺少 `approval_info` 字段**，导致后端无法保存审批人信息。

### 3. 数据流程

#### 前端流程（SetTaskDialog.vue）
1. 用户在"审批设置"标签页选择"需要审批"
2. 选择审批人类型（指定用户/角色/部门/发起人）
3. 选择具体的审批人
4. 点击"确定"按钮，触发 `handleCreate()` 方法
5. 构建 `approvalInfo` 对象，包含：
   - `dataType`: 审批人类型（USERS/ROLES/DEPTS/INITIATOR）
   - `assignee`: 单个审批人ID（如果只有1个用户）
   - `candidateUsers`: 多个审批人ID（逗号分隔）
   - `candidateGroups`: 角色或部门ID（逗号分隔）
   - `text`: 审批人显示名称
6. 调用 `updateApprovalSetApi` 发送请求到 `/project/task/updateApprovalSet`

#### 后端流程
1. `ProjectTaskController.updateApprovalSet` 接收请求
2. 调用 `wfDeployService.updateApprovalSet2`（Feign远程调用）
3. `WfDeployController.updateApprovalSet2` 处理请求
4. `WfDeployServiceImpl.updateApprovalSet2` 保存到数据库：
   - 查询或创建 `pmhub_wf_approval_set` 记录
   - 保存 `approval_info` 字段（JSON字符串）

#### 审批人应用到流程任务
当任务启动流程时：
1. `WfProcessServiceImpl.applyApprovalInfoToTask` 方法读取审批设置
2. 解析 `approval_info` JSON数据
3. 根据类型设置到流程引擎：
   - **USERS**: 使用 `taskService.setAssignee()` 或 `taskService.addCandidateUser()`
   - **ROLES**: 使用 `taskService.addCandidateGroup()`
   - **DEPTS**: 使用 `taskService.addCandidateGroup()`
   - **INITIATOR**: 设置为流程发起人

## 数据库表结构

### pmhub_wf_approval_set 表
```sql
CREATE TABLE `pmhub_wf_approval_set`
(
    `id`            varchar(32) NOT NULL,
    `type`          varchar(32) DEFAULT NULL,           -- 审批类型（TASK/PROJECT等）
    `approved`      varchar(10) DEFAULT NULL,           -- 是否需要审批（0-需要，1-不需要）
    `deployment_id` varchar(64) DEFAULT NULL,          -- 流程部署ID
    `definition_id` varchar(64) DEFAULT NULL,          -- 流程定义ID
    `created_by`    varchar(64) DEFAULT NULL,
    `created_time`  datetime    DEFAULT NULL,
    `updated_by`    varchar(64) DEFAULT NULL,
    `updated_time`  datetime    DEFAULT NULL,
    `extra_id`      varchar(32) DEFAULT NULL,          -- 任务ID或项目ID
    `approval_info` text DEFAULT NULL,                 -- 审批人信息（JSON字符串）⭐ 新增字段
    PRIMARY KEY (`id`) USING BTREE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### approval_info 字段格式示例

**指定用户（单个）：**
```json
{
  "dataType": "USERS",
  "assignee": "123",
  "candidateUsers": "",
  "candidateGroups": "",
  "text": "张三"
}
```

**指定用户（多个）：**
```json
{
  "dataType": "USERS",
  "assignee": "",
  "candidateUsers": "123,456,789",
  "candidateGroups": "",
  "text": "张三,李四,王五"
}
```

**角色：**
```json
{
  "dataType": "ROLES",
  "assignee": "",
  "candidateUsers": "",
  "candidateGroups": "ROLE1,ROLE2",
  "text": "项目经理,部门经理"
}
```

**部门：**
```json
{
  "dataType": "DEPTS",
  "assignee": "",
  "candidateUsers": "",
  "candidateGroups": "100,101",
  "text": "技术部,产品部"
}
```

**发起人：**
```json
{
  "dataType": "INITIATOR",
  "assignee": "${initiator}",
  "candidateUsers": "",
  "candidateGroups": "",
  "text": "流程发起人"
}
```

## 修复步骤

### 1. 执行数据库迁移脚本
运行 `sql/add_approval_info_column.sql` 为现有数据库添加字段：

```sql
ALTER TABLE `pmhub_wf_approval_set` 
ADD COLUMN `approval_info` text DEFAULT NULL COMMENT '审批人信息（JSON字符串）';
```

或者使用提供的迁移脚本（会自动检查字段是否存在）。

### 2. 验证修复
1. 重启后端服务
2. 打开任务设置，选择"需要审批"
3. 选择审批人并保存
4. 检查数据库 `pmhub_wf_approval_set` 表的 `approval_info` 字段是否有数据
5. 启动流程，验证审批人是否正确应用到流程任务

## 流程任务和审批人的关联

### Flowable 引擎中的关联
审批人信息最终会保存到 Flowable 引擎的表中：

1. **act_ru_identitylink**: 运行时任务候选用户/组
   - `TYPE_`: "candidate"（候选）或 "assignee"（已分配）
   - `USER_ID_`: 用户ID（如果是用户）
   - `GROUP_ID_`: 组ID（如果是角色或部门）

2. **act_ru_task**: 运行时任务表
   - `ASSIGNEE_`: 任务负责人（单个审批人）
   - 通过 `act_ru_identitylink` 关联候选用户和组

### 数据流转过程
```
前端选择审批人 
  ↓
保存到 pmhub_wf_approval_set.approval_info
  ↓
任务启动流程时读取审批设置
  ↓
解析 approval_info JSON
  ↓
调用 Flowable API 设置任务审批人
  ↓
保存到 act_ru_identitylink 和 act_ru_task
```

## 注意事项

1. **字段已修复**：SQL文件已更新，新部署的数据库会自动包含 `approval_info` 字段
2. **现有数据库需要迁移**：如果数据库已存在，需要执行迁移脚本添加字段
3. **审批人信息是JSON格式**：存储在 `approval_info` 字段中，类型为 TEXT
4. **无需审批时**：`approval_info` 会被清空为 NULL
5. **流程启动时应用**：审批人信息在流程启动时才会应用到 Flowable 引擎的任务上

## 验证方法

### 1. 检查数据库
```sql
-- 查看审批设置
SELECT id, extra_id, approved, approval_info 
FROM pmhub_wf_approval_set 
WHERE extra_id = '任务ID';
```

### 2. 检查前端请求
打开浏览器开发者工具，查看 Network 标签：
- 请求URL: `/project/task/updateApprovalSet`
- 请求体应包含 `approvalInfo` 字段（JSON字符串）

### 3. 检查后端日志
查看后端日志，确认：
- 请求是否到达控制器
- 数据是否成功保存到数据库
- 是否有异常错误

## 待办任务说明

### 问题：点击审批后，任务是否会在"我的事务"的"待办任务"中出现？

**答案：是的，但需要满足以下条件：**

1. **必须使用 Flowable 流程引擎**
   - 任务必须有流程定义（`definition_id` 不为空）
   - 如果使用无流程定义的简化版审批（`instance_id` 为 NULL），任务**不会**出现在待办列表中

2. **审批人必须正确设置**
   - 审批人信息必须保存在 `pmhub_wf_approval_set` 表的 `approval_info` 字段中
   - 启动流程时，系统会调用 `applyApprovalInfoToTask()` 方法将审批人设置到 Flowable 任务中

3. **审批人分配方式**
   - **单个审批人**：使用 `taskService.setAssignee(taskId, userId)` 直接分配给用户
   - **多个审批人**：使用 `taskService.addCandidateUser(taskId, userId)` 设置为候选用户
   - **角色/部门**：使用 `taskService.addCandidateGroup(taskId, groupId)` 设置为候选组

4. **待办任务查询逻辑**
   - 后端查询使用 `taskCandidateOrAssigned(TaskUtils.getUserId())` 和 `taskCandidateGroupIn(TaskUtils.getCandidateGroup())`
   - 只有当任务满足以下条件之一时，才会出现在待办列表中：
     - 任务被直接分配给当前用户（`assignee = 当前用户ID`）
     - 当前用户是任务的候选用户（`candidateUser`）
     - 当前用户所属的角色/部门是任务的候选组（`candidateGroup`）

### 待办任务页面位置

- **前端路径**：`/workflow/work/todo`
- **前端组件**：`pmhub-ui/src/views/workflow/work/todo.vue`
- **后端API**：`GET /workflow/process/todoList`
- **后端方法**：`WfProcessServiceImpl.selectTodoProcessList()`

### 验证待办任务是否出现

1. **检查流程实例是否创建**
   ```sql
   SELECT * FROM pmhub_project_task_process 
   WHERE extra_id = '任务ID' AND type = 'task' AND instance_id IS NOT NULL;
   ```

2. **检查 Flowable 任务是否创建**
   ```sql
   SELECT * FROM act_ru_task 
   WHERE proc_inst_id_ = '流程实例ID';
   ```

3. **检查任务是否分配给审批人**
   ```sql
   -- 检查直接分配
   SELECT * FROM act_ru_task 
   WHERE proc_inst_id_ = '流程实例ID' AND assignee_ = '审批人用户ID';
   
   -- 检查候选用户
   SELECT * FROM act_ru_identitylink 
   WHERE task_id_ = '任务ID' AND user_id_ = '审批人用户ID' AND type_ = 'candidate';
   
   -- 检查候选组（角色/部门）
   SELECT * FROM act_ru_identitylink 
   WHERE task_id_ = '任务ID' AND group_id_ = '角色/部门ID' AND type_ = 'candidate';
   ```

4. **检查审批人信息是否正确保存**
   ```sql
   SELECT * FROM pmhub_wf_approval_set 
   WHERE extra_id = '任务ID' AND type = 'task';
   -- 查看 approval_info 字段是否有数据
   ```

### 常见问题排查

1. **任务没有出现在待办列表中**
   - 检查是否有流程定义（`definition_id` 不为空）
   - 检查审批人信息是否正确保存
   - 检查 Flowable 任务是否创建
   - 检查任务是否分配给当前用户

2. **审批人看不到待办任务**
   - 确认审批人用户ID是否正确
   - 确认任务是否已分配给该用户（assignee 或 candidateUser）
   - 确认用户所属的角色/部门是否匹配（如果是按角色/部门分配）

3. **无流程定义的简化版审批**
   - 如果 `instance_id` 为 NULL，说明使用的是简化版审批
   - **新版本已支持简化审批流程**：系统会创建 `pmhub_wf_approval_task` 表中的审批任务
   - 简化审批任务会出现在待办列表中（需要实现待办任务查询逻辑）
   - 详见：`简化审批流程设计文档.md` 和 `简化审批流程实现总结.md`

## 简化审批流程（新功能）

### 概述
为了简化审批流程，系统现在支持**不依赖 Flowable 流程定义（definition_id）**的简化审批流程。

### 特点
1. **无需流程定义**：发起审批时不需要 `definition_id`
2. **直接创建审批任务**：基于审批设置直接创建审批任务到 `pmhub_wf_approval_task` 表
3. **支持多种审批人类型**：用户、角色、部门、发起人
4. **待办任务可见**：审批任务会出现在"我的事务"的"待办任务"中

### 数据库设计
- **新增表**：`pmhub_wf_approval_task`（简化审批任务表）
- **修改表**：`pmhub_project_task_process`（添加 `approval_task_id` 字段）

### 实现状态
- ✅ 数据库设计完成
- ✅ 实体类和Mapper完成
- ✅ 审批流程启动逻辑完成
- ⏳ 待办任务查询逻辑（待实现）
- ⏳ 审批完成逻辑（待实现）

### 相关文档
- `简化审批流程设计文档.md`：详细设计文档
- `简化审批流程实现总结.md`：实现总结和后续工作
- `sql/simplified_approval_design.sql`：数据库迁移脚本


