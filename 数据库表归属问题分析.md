# 数据库表归属问题分析

## 问题描述

发现 `pmhub-project` 数据库和 `pmhub-workflow` 数据库中都存在 `pmhub_project_task_process` 表，需要确认哪张表是正确的。

## 代码分析

### 1. 表的使用位置

- **实体类位置**: `pmhub-base/pmhub-base-core/src/main/java/com/laigeoffer/pmhub/base/core/core/domain/entity/WfTaskProcess.java`
- **Mapper位置**: `pmhub-modules/pmhub-workflow/src/main/java/com/laigeoffer/pmhub/workflow/mapper/WfTaskProcessMapper.java`
- **使用模块**: `pmhub-workflow` 模块（工作流模块）中大量使用 `WfTaskProcessMapper`

### 2. 数据库配置

- **pmhub-project 服务**: 连接到 `pmhub-project` 数据库
- **pmhub-workflow 服务**: 连接到 `pmhub-workflow` 数据库

### 3. SQL脚本分析

- **pmhub-project.sql**: ✅ 包含 `pmhub_project_task_process` 表的定义（第308-324行）
- **pmhub-workflow.sql**: ❌ **不包含** `pmhub_project_task_process` 表的定义

### 4. 代码使用情况

- `pmhub-project` 模块：**没有使用** `WfTaskProcessMapper`（grep 结果为空）
- `pmhub-workflow` 模块：**大量使用** `WfTaskProcessMapper`，包括：
  - `WfProcessServiceImpl` - 流程服务实现
  - `WfDeployServiceImpl` - 部署服务实现
  - `WfTaskServiceImpl` - 任务服务实现
  - `WfModelServiceImpl` - 模型服务实现
  - `WfInstanceServiceImpl` - 实例服务实现
  - `GlobalProcessEndListener` - 流程结束监听器

## 结论

### ✅ 正确的表位置

**`pmhub_project_task_process` 表应该在 `pmhub-workflow` 数据库中**，原因：

1. **代码归属**: 所有使用该表的代码都在 `pmhub-workflow` 模块中
2. **服务归属**: `pmhub-workflow` 服务需要访问这个表来管理审批流程
3. **功能归属**: 这个表存储的是工作流审批流程信息，属于工作流模块的职责范围

### ❌ 错误的表位置

**`pmhub-project` 数据库中的 `pmhub_project_task_process` 表是多余的**，原因：

1. `pmhub-project` 模块不使用这个表
2. `pmhub-project` 服务不访问这个表
3. 表名虽然带有 "project"，但实际存储的是工作流审批信息

## 修复建议

### 方案一：将表添加到 pmhub-workflow.sql（推荐）

1. 将 `pmhub_project_task_process` 表的定义添加到 `sql/pmhub-workflow.sql` 中
2. 从 `sql/pmhub-project.sql` 中删除该表定义（或添加注释说明已被移除）
3. 在 `pmhub-workflow` 数据库中执行建表语句
4. 迁移 `pmhub-project` 数据库中的数据到 `pmhub-workflow` 数据库（如果有数据）
5. 删除 `pmhub-project` 数据库中的表

### 方案二：保持现状（如果两个服务共享数据库）

如果 `pmhub-project` 和 `pmhub-workflow` 服务实际上连接的是同一个数据库实例，那么两张表可能是：
- 不同环境的数据库（如测试环境和生产环境）
- 或者确实是重复创建的错误

## 数据迁移脚本

如果需要迁移数据，可以使用以下脚本：

```sql
-- 1. 检查两个数据库中的数据
-- 在 pmhub-project 数据库中执行
SELECT COUNT(*) as project_count FROM pmhub_project_task_process;

-- 在 pmhub-workflow 数据库中执行
SELECT COUNT(*) as workflow_count FROM pmhub_project_task_process;

-- 2. 如果 pmhub-project 数据库中有数据，需要迁移到 pmhub-workflow 数据库
-- 注意：需要确保两个数据库中的表结构一致

-- 3. 迁移完成后，删除 pmhub-project 数据库中的表
-- DROP TABLE IF EXISTS `pmhub_project_task_process`;
```

## 注意事项

1. **数据备份**: 在执行任何删除操作之前，务必先备份数据
2. **数据一致性**: 如果两个数据库中有不同的数据，需要确认如何合并
3. **服务重启**: 修复后可能需要重启相关服务
4. **SQL脚本更新**: 更新 SQL 脚本，确保未来部署时不会再次创建错误的表


