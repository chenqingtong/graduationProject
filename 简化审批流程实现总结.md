# 简化审批流程实现总结

## 已完成的工作

### 1. 数据库设计 ✅
- 创建了 `pmhub_wf_approval_task` 表（简化审批任务表）
- 修改了 `pmhub_project_task_process` 表，添加 `approval_task_id` 字段
- SQL脚本：`sql/simplified_approval_design.sql`

### 2. 实体类和Mapper ✅
- 创建了 `WfApprovalTask.java` 实体类
- 创建了 `WfApprovalTaskMapper.java` 接口
- 创建了 `WfApprovalTaskMapper.xml` MyBatis映射文件

### 3. 审批流程启动逻辑 ✅
- 修改了 `startTaskProcessWithoutFlow()` 方法
- 新增了 `createSimplifiedApprovalTasks()` 方法，支持：
  - 指定用户（单个/多个）
  - 角色（查询角色下的所有用户）
  - 部门（查询部门下的所有用户）
  - 发起人

### 4. 设计文档 ✅
- 创建了详细的设计文档：`简化审批流程设计文档.md`

## 待完成的工作

### 1. 待办任务查询逻辑 ⏳
需要修改 `selectTodoProcessList()` 和 `selectPageTodoProcessList()` 方法，使其同时查询：
- Flowable 待办任务（原有逻辑）
- 简化审批待办任务（新逻辑）

**实现思路：**
```java
@Override
public List<WfTaskVo> selectTodoProcessList(ProcessQuery processQuery) {
    List<WfTaskVo> taskVoList = new ArrayList<>();
    
    // 1. 查询 Flowable 待办任务（原有逻辑）
    TaskQuery taskQuery = taskService.createTaskQuery()
        .active()
        .includeProcessVariables()
        .taskCandidateOrAssigned(TaskUtils.getUserId())
        .taskCandidateGroupIn(TaskUtils.getCandidateGroup())
        .orderByTaskCreateTime().desc();
    ProcessUtils.buildProcessSearch(taskQuery, processQuery);
    List<Task> taskList = taskQuery.list();
    // 转换为 WfTaskVo...
    
    // 2. 查询简化审批待办任务（新逻辑）
    Long userId = SecurityUtils.getUserId();
    SysUser user = wfCopyMapper.selectUserById(userId);
    List<Long> roleIds = getUserRoleIds(userId);
    List<Long> deptIds = getUserDeptIds(user);
    
    List<WfApprovalTask> approvalTasks = wfApprovalTaskMapper.selectTodoListByUserId(
        userId.toString(), roleIds, deptIds);
    
    // 转换为 WfTaskVo 并合并到 taskVoList
    
    return taskVoList;
}
```

### 2. 审批完成逻辑 ⏳
需要创建新的接口和方法来处理简化审批任务的完成：

**需要创建的接口：**
```java
// 在 WfProcessController 中
@PostMapping("/completeSimplifiedApproval")
public R completeSimplifiedApproval(@RequestBody ApprovalCompleteDTO dto) {
    wfProcessService.completeSimplifiedApproval(dto);
    return R.ok();
}
```

**需要实现的方法：**
```java
// 在 WfProcessServiceImpl 中
public void completeSimplifiedApproval(ApprovalCompleteDTO dto) {
    // 1. 查询审批任务
    WfApprovalTask task = wfApprovalTaskMapper.selectById(dto.getApprovalTaskId());
    if (task == null || !"pending".equals(task.getStatus())) {
        throw new ServiceException("审批任务不存在或已处理");
    }
    
    // 2. 更新审批任务状态
    String status = dto.getApproved() ? "approved" : "rejected";
    wfApprovalTaskMapper.updateStatus(task.getId(), status, dto.getComment());
    
    // 3. 如果是拒绝，取消其他待审批任务
    if ("rejected".equals(status)) {
        wfApprovalTaskMapper.cancelPendingTasks(task.getExtraId(), task.getType());
        // 更新业务对象状态
        updateBusinessObjectStatus(task.getExtraId(), "rejected");
    } else {
        // 4. 如果是通过，检查是否所有审批人都已通过
        int pendingCount = wfApprovalTaskMapper.countPendingByExtraIdAndType(
            task.getExtraId(), task.getType());
        if (pendingCount == 0) {
            // 所有审批人都已通过，更新业务对象状态
            updateBusinessObjectStatus(task.getExtraId(), "approved");
        }
    }
    
    // 5. 更新 pmhub_project_task_process 的 approved 字段
    updateWfTaskProcessApproved(task.getExtraId(), task.getType(), status);
}
```

### 3. 前端接口对接 ⏳
需要在前端添加：
- 待办任务列表显示简化审批任务
- 审批操作界面（通过/拒绝）
- 审批详情查看

### 4. 数据库迁移 ⏳
需要执行 SQL 脚本：
```sql
-- 执行 sql/simplified_approval_design.sql
```

### 5. 测试验证 ⏳
- 测试无流程定义的审批流程启动
- 测试待办任务查询
- 测试审批完成（通过/拒绝）
- 测试多人审批场景

## 关键代码位置

### 已修改的文件
1. `pmhub-modules/pmhub-workflow/src/main/java/com/laigeoffer/pmhub/workflow/service/impl/WfProcessServiceImpl.java`
   - 修改了 `startTaskProcessWithoutFlow()` 方法
   - 新增了 `createSimplifiedApprovalTasks()` 方法
   - 新增了 `createApprovalTask()` 方法

### 新增的文件
1. `pmhub-modules/pmhub-workflow/src/main/java/com/laigeoffer/pmhub/workflow/domain/WfApprovalTask.java`
2. `pmhub-modules/pmhub-workflow/src/main/java/com/laigeoffer/pmhub/workflow/mapper/WfApprovalTaskMapper.java`
3. `pmhub-modules/pmhub-workflow/src/main/resources/mapper/WfApprovalTaskMapper.xml`
4. `sql/simplified_approval_design.sql`
5. `简化审批流程设计文档.md`

## 注意事项

1. **审批人类型处理**：
   - `USERS`: 直接使用用户ID
   - `ROLES`: 需要查询角色下的所有用户（已实现）
   - `DEPTS`: 需要查询部门下的所有用户（已实现）
   - `INITIATOR`: 使用流程发起人ID（已实现）

2. **多人审批处理**：
   - 当前实现支持会签（所有人通过才算通过）
   - 如需支持或签，需要修改审批完成逻辑

3. **审批顺序**：
   - 当前实现不支持审批顺序，所有审批人同时收到审批任务
   - 如需支持顺序审批，可以添加 `approval_order` 字段

4. **兼容性**：
   - 代码中通过判断 `definition_id` 是否为空来决定使用哪种审批方式
   - 两种方式可以并存，不影响现有功能

## 下一步行动

1. 实现待办任务查询逻辑（修改 `selectTodoProcessList()` 方法）
2. 实现审批完成逻辑（新增 `completeSimplifiedApproval()` 方法）
3. 创建审批完成接口（在 Controller 中）
4. 执行数据库迁移脚本
5. 测试验证功能

